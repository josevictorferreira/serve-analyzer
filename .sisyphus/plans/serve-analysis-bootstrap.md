## TODOs

- [x] T1. Add a `flake.nix` dev shell with Python, Jupyter, and video-analysis deps; confirm `nix develop` works.
- [x] T2. Add an initial Python serve-analysis script that loads a video, lets the user define a 2-point manual scale, tracks the ball after impact, and estimates post-impact velocity.
- [x] T3. Add a Jupyter notebook for exploratory analysis and parameter tuning using the same workflow.
- [x] T4. Add minimal usage docs for the dev shell, script, calibration flow, and notebook.

## Final Verification Wave

- [x] F1. Code reviewer verdict on correctness and scope.
- [x] F2. Environment reviewer verdict on flake + nix develop workflow.
- [x] F3. UX reviewer verdict on notebook/script usability for manual calibration.
- [x] F4. Final integration verdict on end-to-end project readiness.
